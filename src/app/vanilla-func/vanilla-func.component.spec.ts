/*
import { Spectator, createComponentFactory } from '@ngneat/spectator';

import { VanillaFuncComponent } from './vanilla-func.component';

describe('VanillaFuncComponent', () => {
  let spectator: Spectator<VanillaFuncComponent>;

  const createComponent = createComponentFactory(VanillaFuncComponent);

  it('should create', () => {
    spectator = createComponent();

    expect(spectator.component).toBeTruthy();
  });
});
*/
