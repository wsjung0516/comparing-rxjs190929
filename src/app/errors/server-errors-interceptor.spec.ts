import { ServerErrorsInterceptor } from './server-errors-interceptor';

describe('ServerErrorsInterceptor', () => {
  it('should create an instance', () => {
    expect(new ServerErrorsInterceptor()).toBeTruthy();
  });
});
